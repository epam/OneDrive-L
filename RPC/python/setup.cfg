
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = src/onedrive_client/rpc/_version.py
versionfile_build = onedrive_client/entities/_version.py
tag_prefix =
